# Решение 5.8. Контрольная 2: SELECT и DML

Контрольная имеет индивидуальные варианты. Ниже показан каркас ответа, а конкретный заполненный пример вынесен в `example_artifacts.md`.

## Минимальный правильный `solution.sql`

```sql
-- task_01
-- filter + ORDER BY + LIMIT, без hard-code строк.

-- task_02
-- корректная фильтрация маршрута или рейсов по параметрам варианта.

-- task_03
-- join-chain только через реальные связи, например tickets -> segments -> flights.

-- task_04
-- GROUP BY на заявленном уровне детализации.

-- task_05
-- EXISTS или NOT EXISTS для условия существования/отсутствия связанных строк.

-- task_06
-- предварительный SELECT;
-- DML с точным predicate;
-- RETURNING или проверочный SELECT.
```

## Обязательные объяснения

- Почему join-chain корректна.
- Что является единицей агрегации.
- Какие строки затрагивает DML.
- Почему ошибочный SQL из варианта дает неверный результат.
