relations:
  airports: Airports
  routes: Routes
  flights: Flights
  tickets: Tickets
  segments: Segments
  required: RequiredFlights
tasks:
  required_result_tables:
    - task_1
    - task_2
    - task_3
    - task_5
  alternative_expression_required_for:
    - task_3
rules:
  sql_authoring_required: false
  allowed_notation:
    - ascii
    - unicode
